# Use Case: Monitorización de Alarmas de Seguridad

## Metadata

| Propiedad        | Valor                                                  |
| ---------------- | ------------------------------------------------------ |
| UC ID            | UC-007                                                 |
| Reglas           | (monitorización — sin regla formal extraída)           |
| Actor primario   | Operador de seguridad (usuario humano en terminal)     |
| Alcance          | Sistema — SICAWeb (`Alarmes.ascx`, `PagAlarmes.aspx`)  |
| Nivel            | User Goal                                              |
| Estado           | Draft — extraído de legacy                             |
| Fuente           | `demo/from_old_src/SICAWeb/SICAWeb/Alarmes.ascx.vb`    |

## Descripción

El operador visualiza un panel de alarmas de seguridad que muestra las alarmas de los últimos
N días (configurable), con refresco automático periódico y marca de tiempo de la última
actualización. Permite la vigilancia continua de eventos anómalos.

## Partes interesadas e intereses

| Parte interesada       | Interés                                                         |
| ---------------------- | --------------------------------------------------------------- |
| Operador de seguridad  | Detección inmediata de alarmas para reacción rápida             |
| Gestión de seguridad   | Registro y seguimiento de incidentes de acceso                  |

## Precondiciones

1. El operador ha superado [UC-003](UC-003.md) (sesión válida).
2. Existe configuración `AlarmDaysToShow` y `AlarmsRefreshInterval` en `AppSettings`.

## Postcondiciones (garantías de éxito)

1. Se muestran las alarmas de la ventana temporal configurada (`AlarmDaysToShow` días).
2. El panel se refresca automáticamente según `AlarmsRefreshInterval`.
3. Se muestra la marca de tiempo de la última actualización (si `ShowRefreshTime`).

## Disparadores

- Carga del control `Alarmes.ascx`.
- Tick del temporizador `TimerAlarmes`.

## Flujo principal de éxito

| Paso | Actor    | Sistema                                                              |
| ---- | -------- | ------------------------------------------------------------------- |
| 1    | Operador | Accede al panel de alarmas                                          |
| 2    |          | Lee `AlarmDaysToShow` y fija el rango temporal a mostrar             |
| 3    |          | Configura `TimerAlarmes.Interval` desde `AlarmsRefreshInterval`      |
| 4    |          | Muestra la marca de tiempo si `ShowRefreshTime` está activo          |
| 5    |          | Renderiza la lista de alarmas de la ventana temporal                 |
| 6    |          | Cada N segundos refresca automáticamente la lista                    |

### Detalle de pasos

**Paso 2 — Ventana temporal**

- `lblNumDias.Text = AppSettings("AlarmDaysToShow")` define cuántos días hacia atrás se muestran.

**Paso 3 — Refresco**

- `TimerAlarmes.Interval` se establece desde `AlarmsRefreshInterval` (en segundos).

## Extensiones (flujos alternativos)

### 5a. Sin alarmas en la ventana

| Paso | Sistema                                          |
| ---- | ------------------------------------------------ |
| 5a.1 | No hay alarmas en el rango configurado           |
| 5a.2 | Se muestra una lista vacía                        |

### *a. Configuración ausente

| Paso | Sistema                                                       |
| ---- | ------------------------------------------------------------- |
| *a.1 | `AppSettings("AlarmDaysToShow")` no definido                  |
| *a.2 | Se usa el valor por defecto del control (comportamiento implícito) |

## Requisitos especiales

- **Disponibilidad**: La monitorización de alarmas es crítica para la operación de seguridad.
- **Configurabilidad**: Rango temporal e intervalo de refresco vía `AppSettings`.

## Frecuencia estimada

- Visualización continua durante el turno.

## Cuestiones abiertas

- ¿De qué fuente se obtienen las alarmas (la lógica de datos no está en el code-behind)?
- ¿Existe un flujo de reconocimiento/resolución de alarmas en el legacy?
