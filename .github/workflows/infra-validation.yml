# AURORA-IA / AI-DLC Infrastructure Validation Pipeline
# This workflow validates Infrastructure as Code changes

name: Infrastructure Validation

on:
  push:
    paths:
      - 'infra/**'
      - 'terraform/**'
      - 'bicep/**'
      - 'kubernetes/**'
      - 'helm/**'
  pull_request:
    paths:
      - 'infra/**'
      - 'terraform/**'
      - 'bicep/**'
      - 'kubernetes/**'
      - 'helm/**'
  workflow_dispatch:

env:
  TF_VERSION: '1.6.0'

jobs:
  # ============================================================================
  # Terraform Validation
  # ============================================================================
  terraform-validate:
    name: Terraform Validation
    runs-on: ubuntu-latest
    if: hashFiles('terraform/**') != '' || hashFiles('infra/**/*.tf') != ''
    defaults:
      run:
        working-directory: terraform
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Terraform
        uses: hashicorp/setup-terraform@v3
        with:
          terraform_version: ${{ env.TF_VERSION }}

      - name: Terraform Format Check
        run: terraform fmt -check -recursive
        continue-on-error: true

      - name: Terraform Init
        run: terraform init -backend=false

      - name: Terraform Validate
        run: terraform validate

      - name: TFLint
        uses: terraform-linters/setup-tflint@v4
        with:
          tflint_version: latest

      - name: Run TFLint
        run: |
          tflint --init
          tflint --recursive

      - name: Checkov Security Scan
        uses: bridgecrewio/checkov-action@v12
        with:
          directory: terraform
          framework: terraform
          output_format: sarif
          output_file_path: checkov-terraform.sarif
        continue-on-error: true

      - name: Upload Checkov results
        uses: github/codeql-action/upload-sarif@v3
        if: always()
        with:
          sarif_file: checkov-terraform.sarif

  # ============================================================================
  # Terraform Plan (on PR)
  # ============================================================================
  terraform-plan:
    name: Terraform Plan
    runs-on: ubuntu-latest
    needs: [terraform-validate]
    if: github.event_name == 'pull_request'
    environment: terraform-plan
    defaults:
      run:
        working-directory: terraform
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Terraform
        uses: hashicorp/setup-terraform@v3
        with:
          terraform_version: ${{ env.TF_VERSION }}

      - name: Terraform Init
        run: terraform init
        env:
          ARM_CLIENT_ID: ${{ secrets.ARM_CLIENT_ID }}
          ARM_CLIENT_SECRET: ${{ secrets.ARM_CLIENT_SECRET }}
          ARM_SUBSCRIPTION_ID: ${{ secrets.ARM_SUBSCRIPTION_ID }}
          ARM_TENANT_ID: ${{ secrets.ARM_TENANT_ID }}

      - name: Terraform Plan
        id: plan
        run: |
          terraform plan -no-color -out=tfplan 2>&1 | tee plan-output.txt
          echo "plan<<EOF" >> $GITHUB_OUTPUT
          cat plan-output.txt >> $GITHUB_OUTPUT
          echo "EOF" >> $GITHUB_OUTPUT
        env:
          ARM_CLIENT_ID: ${{ secrets.ARM_CLIENT_ID }}
          ARM_CLIENT_SECRET: ${{ secrets.ARM_CLIENT_SECRET }}
          ARM_SUBSCRIPTION_ID: ${{ secrets.ARM_SUBSCRIPTION_ID }}
          ARM_TENANT_ID: ${{ secrets.ARM_TENANT_ID }}

      - name: Comment Plan on PR
        uses: actions/github-script@v7
        with:
          script: |
            const output = `## Terraform Plan
            
            \`\`\`
            ${{ steps.plan.outputs.plan }}
            \`\`\`
            
            *Pusher: @${{ github.actor }}, Action: \`${{ github.event_name }}\`*`;
            
            github.rest.issues.createComment({
              issue_number: context.issue.number,
              owner: context.repo.owner,
              repo: context.repo.repo,
              body: output.substring(0, 65000)  // GitHub comment limit
            });

  # ============================================================================
  # Bicep Validation
  # ============================================================================
  bicep-validate:
    name: Bicep Validation
    runs-on: ubuntu-latest
    if: hashFiles('bicep/**') != '' || hashFiles('infra/**/*.bicep') != ''
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Bicep
        uses: azure/setup-bicep@v0.1

      - name: Build Bicep files
        run: |
          find . -name "*.bicep" -exec bicep build {} \;

      - name: Lint Bicep files
        run: |
          find . -name "*.bicep" -exec bicep lint {} \;

      - name: Checkov Security Scan
        uses: bridgecrewio/checkov-action@v12
        with:
          directory: .
          framework: bicep
        continue-on-error: true

  # ============================================================================
  # Kubernetes Manifests Validation
  # ============================================================================
  kubernetes-validate:
    name: Kubernetes Validation
    runs-on: ubuntu-latest
    if: hashFiles('kubernetes/**') != '' || hashFiles('k8s/**') != ''
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup kubectl
        uses: azure/setup-kubectl@v3

      - name: Validate manifests
        run: |
          find kubernetes -name "*.yaml" -o -name "*.yml" | while read file; do
            echo "Validating $file..."
            kubectl apply --dry-run=client -f "$file" 2>&1 || true
          done

      - name: KubeLinter
        uses: stackrox/kube-linter-action@v1
        with:
          directory: kubernetes
          config: .kube-linter.yaml
        continue-on-error: true

      - name: Kubesec Scan
        run: |
          curl -sSX POST --data-binary @kubernetes/deployment.yaml https://v2.kubesec.io/scan || true

      - name: Checkov Kubernetes Scan
        uses: bridgecrewio/checkov-action@v12
        with:
          directory: kubernetes
          framework: kubernetes
        continue-on-error: true

  # ============================================================================
  # Helm Chart Validation
  # ============================================================================
  helm-validate:
    name: Helm Validation
    runs-on: ubuntu-latest
    if: hashFiles('helm/**') != '' || hashFiles('charts/**') != ''
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Helm
        uses: azure/setup-helm@v3

      - name: Lint Helm charts
        run: |
          find . -name "Chart.yaml" -exec dirname {} \; | while read chart; do
            echo "Linting $chart..."
            helm lint "$chart"
          done

      - name: Template Helm charts
        run: |
          find . -name "Chart.yaml" -exec dirname {} \; | while read chart; do
            echo "Templating $chart..."
            helm template test "$chart" > /dev/null
          done

      - name: Helm unittest
        run: |
          helm plugin install https://github.com/helm-unittest/helm-unittest.git || true
          find . -name "Chart.yaml" -exec dirname {} \; | while read chart; do
            if [ -d "$chart/tests" ]; then
              helm unittest "$chart"
            fi
          done

  # ============================================================================
  # Docker Validation
  # ============================================================================
  docker-validate:
    name: Docker Validation
    runs-on: ubuntu-latest
    if: hashFiles('Dockerfile*') != '' || hashFiles('**/Dockerfile*') != ''
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Hadolint Dockerfile Linter
        uses: hadolint/hadolint-action@v3.1.0
        with:
          recursive: true
          failure-threshold: warning

      - name: Build test image
        run: |
          if [ -f "Dockerfile" ]; then
            docker build --target test -t test-image . || docker build -t test-image .
          fi

      - name: Trivy Image Scan
        uses: aquasecurity/trivy-action@master
        if: hashFiles('Dockerfile') != ''
        with:
          image-ref: 'test-image'
          format: 'sarif'
          output: 'trivy-docker.sarif'
        continue-on-error: true

  # ============================================================================
  # Cost Estimation
  # ============================================================================
  cost-estimation:
    name: Infrastructure Cost Estimation
    runs-on: ubuntu-latest
    needs: [terraform-validate]
    if: github.event_name == 'pull_request'
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Infracost
        uses: infracost/actions/setup@v3
        with:
          api-key: ${{ secrets.INFRACOST_API_KEY }}

      - name: Generate cost breakdown
        run: |
          infracost breakdown --path=terraform --format=json --out-file=/tmp/infracost.json || true

      - name: Post cost comment
        uses: infracost/actions/comment@v1
        if: hashFiles('/tmp/infracost.json') != ''
        with:
          path: /tmp/infracost.json
          behavior: update

  # ============================================================================
  # Summary
  # ============================================================================
  infra-summary:
    name: Infrastructure Summary
    runs-on: ubuntu-latest
    needs: [terraform-validate, bicep-validate, kubernetes-validate, helm-validate, docker-validate]
    if: always()
    steps:
      - name: Generate summary
        run: |
          echo "## Infrastructure Validation Summary" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "| Component | Status |" >> $GITHUB_STEP_SUMMARY
          echo "|-----------|--------|" >> $GITHUB_STEP_SUMMARY
          echo "| Terraform | ${{ needs.terraform-validate.result || 'skipped' }} |" >> $GITHUB_STEP_SUMMARY
          echo "| Bicep | ${{ needs.bicep-validate.result || 'skipped' }} |" >> $GITHUB_STEP_SUMMARY
          echo "| Kubernetes | ${{ needs.kubernetes-validate.result || 'skipped' }} |" >> $GITHUB_STEP_SUMMARY
          echo "| Helm | ${{ needs.helm-validate.result || 'skipped' }} |" >> $GITHUB_STEP_SUMMARY
          echo "| Docker | ${{ needs.docker-validate.result || 'skipped' }} |" >> $GITHUB_STEP_SUMMARY
